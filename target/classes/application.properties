# Server Configuration
server.port=${PORT:8081}

# Database Configuration
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/farmterra}
spring.datasource.username=${DATABASE_USERNAME:farmterra_user}
spring.datasource.password=${DATABASE_PASSWORD:yGHdvCbwRVP4EpwOT0fSYovsyr4Hs68K}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=${JWT_SECRET}
jwt.expiration=86400000

# Logging
logging.level.org.springframework.security=INFO
logging.level.com.farmterra=INFO

# CORS Configuration
spring.mvc.cors.enabled=true
